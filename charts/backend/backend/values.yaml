replicaCount: 1

image:
  repository: nginx
  tag: "1.27"

service:
  type: ClusterIP
  port: 80

resources: {}


serviceAccount:
  create: true
  name: ""

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 5
