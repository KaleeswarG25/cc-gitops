apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloudcart.fullname" . }}-test-connection"
  labels:
    {{- include "cloudcart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cloudcart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
