apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudcart.fullname" . }}
  labels:
    {{- include "cloudcart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloudcart.selectorLabels" . | nindent 4 }}
